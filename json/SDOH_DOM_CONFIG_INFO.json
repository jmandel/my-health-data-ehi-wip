{
  "$meta": {
    "schemas": {
      "SDOH_DOM_CONFIG_INFO": {
        "name": "SDOH_DOM_CONFIG_INFO",
        "description": "This table stores the social determinant domain configuration display information for domains configured for use in social determinants of health workflows.",
        "primaryKey": [
          {
            "columnName": "DOM_CONFIG_ID",
            "ordinalPosition": 1
          }
        ],
        "columns": [
          {
            "ordinalPosition": 1,
            "name": "DOM_CONFIG_ID",
            "type": "NUMERIC",
            "discontinued": false,
            "description": "The unique identifier (.1 item) for the domain configuration record."
          },
          {
            "ordinalPosition": 2,
            "name": "RECORD_NAME",
            "type": "VARCHAR",
            "discontinued": false,
            "description": "The name of the social determinant domain configuration record."
          },
          {
            "ordinalPosition": 3,
            "name": "DISPLAY_NAME",
            "type": "VARCHAR",
            "discontinued": false,
            "description": "The display name of the social determinant domain configuration record."
          }
        ]
      }
    }
  },
  "SDOH_DOM_CONFIG_INFO": [
    {
      "DOM_CONFIG_ID": 97003,
      "RECORD_NAME": "Tobacco Use - Patient History",
      "DISPLAY_NAME": "Patient History",
      "$meta": {
        "type": "SDOH_DOM_CONFIG_INFO"
      }
    },
    {
      "DOM_CONFIG_ID": 97005,
      "RECORD_NAME": "Patient Health Questionnaire-2 (PHQ-2)",
      "DISPLAY_NAME": "PHQ-2",
      "$meta": {
        "type": "SDOH_DOM_CONFIG_INFO"
      }
    }
  ]
}